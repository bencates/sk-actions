export {}

// import type { FormActionHandler } from '$lib/index.js'

// export const actions: Record<string, FormActionHandler<unknown>> = {
// 	helloWorld: ({ submit, fields }) => submit(fields),
// }
